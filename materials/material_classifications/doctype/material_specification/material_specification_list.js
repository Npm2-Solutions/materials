frappe.listview_settings["Material Specification"] = {
	// Fetch `enabled` so the quick-list indicator resolves to "Enabled" (blue),
	// not a false grey "Disabled" (see guide 09 / welding_code_list.js).
	add_fields: ["enabled"],
};
